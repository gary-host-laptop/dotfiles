# nushell generated init + history (mirrors .gitignore — chezmoi ignores .gitignore, so these must be listed here too)
# NOTE: patterns match against the TARGET path (not the source path)
.config/nushell/history.txt
.config/nushell/starship-init.nu
.config/nushell/zoxide.nu

# superseedr runtime state — regenerated by the app, only settings.toml is managed
.config/jagalite.superseedr/torrent_metadata.toml
.config/jagalite.superseedr/backups_settings_files/

# repo-root docs that belong in the repo but not mirrored into $HOME
README.md
LICENSE
